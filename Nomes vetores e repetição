var nome, nomes = []
var cont = 0

while (true) {

  nome = prompt("Digite um nome para salvar. Digite \"fim\" para parar")

  if (nome == "fim") {
    break
  }

  nomes.push(nome)



}

while (cont < nomes.length) {
  console.log(nomes[cont])
  cont++
}
